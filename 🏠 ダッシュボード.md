---
tags:
  - dashboard
---

# 🏠 ダッシュボード

[[2026_GOAL|🎯 2026目標]]｜[[転職ボード|💼 転職ボード]]｜[[支出管理|💰 家計簿ルール]]｜[[心得|🧭 心得]]

---

## 🏃 運動30分(今年)

```dataviewjs
const data = { year: 2026, colors: { green: ["#c6e48b","#7bc96f","#239a3b","#196127"] }, entries: [] }
for (let page of dv.pages('"2_daily"')) {
  const done = page.file.tasks.filter(t => t.text.includes("運動30分") && t.completed)
  const m = page.file.name.match(/^(\d{2})-(\d{2})-(\d{4})$/)
  if (done.length && m) data.entries.push({ date: `${m[3]}-${m[1]}-${m[2]}`, intensity: 1 })
}
renderHeatmapCalendar(this.container, data)
```

## 📚 読書15分(今年)

```dataviewjs
const data = { year: 2026, colors: { blue: ["#9be9f8","#54c8f0","#2596be","#145d80"] }, entries: [] }
for (let page of dv.pages('"2_daily"')) {
  const done = page.file.tasks.filter(t => t.text.includes("読書15分") && t.completed)
  const m = page.file.name.match(/^(\d{2})-(\d{2})-(\d{4})$/)
  if (done.length && m) data.entries.push({ date: `${m[3]}-${m[1]}-${m[2]}`, intensity: 1 })
}
renderHeatmapCalendar(this.container, data)
```

## ⚖️ 体重(日記の「体重::」欄から自動)

```tracker
searchType: dvField
searchTarget: 体重
folder: 2_daily
dateFormat: MM-DD-YYYY
line:
    title: 体重の推移
    yAxisLabel: kg
    lineColor: "#e0699a"
    showPoint: true
```

---

## 🗂 月次レビュー

- 最新: [[2026-06_月次レビュー]]
- 毎月1日 9:30に前月分が `2_daily/_review/` に自動生成されます
